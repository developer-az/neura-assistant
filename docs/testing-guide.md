# Testing

```bash
npm test
npm run type-check
```

Core coverage today: week scoring helpers (`clampScore`, deltas, Monday week keys).

Manual smoke:

1. Claim notebook → add focus → add attributes with starting scores
2. Confirm scores on home
3. Weekly check-in with +/− and typed absolute values
4. Toggle Draw, scribble, Done, leave and return (strokes persist)
